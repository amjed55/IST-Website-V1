'use client';

/** @deprecated Hero choreography lives in PageHero — kept for any leftover imports */
export function HeroReveal({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
