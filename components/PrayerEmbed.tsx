'use client';

/**
 * Prayer board embed on /prayer-times.
 * Renders the Classic Prayer Clock layout natively (no external iframe).
 */
export { ClassicPrayerBoard as PrayerEmbed, ClassicPrayerBoard } from './ClassicPrayerBoard';
